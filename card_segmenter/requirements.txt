# ---------------------------------------------------------------------------
# PyTorch is NOT listed here on purpose.
# Install it matched to your machine using the official selector:
#     https://pytorch.org/get-started/locally/
# On Google Colab, torch + torchvision are ALREADY installed — do not reinstall.
# A recent stable (torch >= 2.2) is fine for everything in this repo.
# ---------------------------------------------------------------------------

# Load-bearing libraries, pinned to versions verified to work with this code:
segmentation-models-pytorch==0.5.0
albumentations==2.0.8

# Supporting libraries (ranges, so the resolver doesn't fight your torch build).
# After your first successful run, freeze exact versions with:  pip freeze > requirements.lock
opencv-python-headless>=4.10
numpy>=1.26,<3
pyyaml>=6.0
tqdm>=4.66
onnx>=1.16
onnxruntime>=1.18
